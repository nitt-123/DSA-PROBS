#include<iostream>
using namespace std;
int gcd(int a,int b){
if(b == 0){
  return a;
  }
  return gcd(b,a%b);
  }

int main(){
int a,b;
  cout<<"enter the numbers to calculate GCD"<<endl;
  cin>>a>>b;
  int k = gcd(a,b);
  cout<<"the GCD of the numbers would be"<<k<<endl;
  return 0;
  }
  
